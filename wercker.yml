box: node:6-slim

dev:
  steps:
    - npm-install
    - internal/watch:
        code: gulp build
        reload: true

build:
  steps:
    - npm-install
    - bower-install
    - hgen/gulp:
        tasks: default
